/** Re-export shared ladder — source of truth is @canifly/middleware. */
export {
  BADGE_HOURS_BONUS,
  MAX_RANK_INDEX,
  PILOT_RANKS,
  computePilotProgress,
  computePilotXp,
  effectiveRankHours,
  hoursFromDurationS,
  levelFromXp,
  rankDefById,
  rankFromHours,
  type PilotRankDef,
  type PilotRankId,
  type PilotRankStats,
  type RankInsignia,
} from "@canifly/middleware";
